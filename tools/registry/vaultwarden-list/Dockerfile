# Image partagée avec vaultwarden-get / vaultwarden-set.
# Le sources sont dans tools/registry/_shared/vaultwarden — build depuis ce dossier.
FROM scratch
LABEL deprecated="Build via : docker build -t spouet/tool-vaultwarden:0.1.0 tools/registry/_shared/vaultwarden"
